{
  "name": "backend",
  "license": "UNLICENSED",
  "scripts": {
    "gen-types": "supabase gen types typescript --local --schema public --schema auth --schema acl --schema storage --schema chat --schema feed > types.d.ts",
    "deno": "deno cache --config functions/deno.json functions/**/index.ts",
    "db-reset": "supabase db reset && pnpm gen-types"
  },
  "devDependencies": {
    "supabase": "^1.192.5"
  }
}
