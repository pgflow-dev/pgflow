---
title: React support
---

import { Code, Steps } from "@astrojs/starlight/components";

pgflow works beautifully with React.
Click **this button** to start the flow.

```typescript

const GenerateSocialPostComponent = () => {
  const { flow } = useFlow(GenerateSocialPost)

  if (flowState.isCompleted('extractTags')) {
    return '';
  }

  return (
    <div>
      <input
        value={inputValue}
        onChange={(e) => setInputValue(e.target.value)}
        placeholder="Enter a number"
      />
      <button onClick={() => flow.run(inputValue)}>Submit</button>
      <p>{result}</p>
    </div>
  );



























const SimpleFlow = () => {
  const [inputValue, setInputValue] = useState('');
  const [result, setResult] = useState('');

  const handleSubmit = () => {
    // Handle the submission here
    setResult(`Submitted value: ${inputValue}`);
  };

  return (
    <div>
      <input
        value={inputValue}
        onChange={(e) => setInputValue(e.target.value)}
        placeholder="Enter a number"
      />
      <button onClick={handleSubmit}>Submit</button>
      <p>{result}</p>
    </div>
  );
};
```

1. ## React - first class citizen

```typescript

```

We just created a new flow with a single step Flow is statically typed
and you can only invoke it with a single input of type `number`.

It provid




