---
title: React support
---

import { Code, Steps } from "@astrojs/starlight/components";

## React - first class citizen

pgflow works beautifully with React.
Click this button to start this flow:

```typescript
const IncrementNumberFlow = new Flow<{input: number}>()
   .task("increment", ({input}) => inputNumber + 1)
```

We just created a new flow with a single task Flow is statically typed
and you can only invoke it with a single input of type `number`.

It provid




