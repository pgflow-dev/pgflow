---
title: Startup Compilation
description: How worker startup deploys and verifies flow definitions
sidebar:
  order: 25
---

import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";

When a flow worker starts, the worker compiles or verifies its imported flow definition before any registration or polling. Worker startup is the only supported flow deployment path.

## How It Works

Every startup follows this ordered contract:

```text
extract complete shape
  -> compile or verify under the flow lock
  -> track the worker function
  -> insert the worker row
  -> start polling
```

Compilation happens under the flow's advisory lock, so concurrent workers for the same flow serialize safely. If compilation fails, the worker performs no registration write: the `worker_functions` row and the `pgflow.workers` row stay unchanged, and startup fails.

```d2
...@../../../assets/pgflow-theme.d2

direction: right

start: "Worker\nstarts"
start.class: step_started

exists: "Exists?"
exists.shape: diamond
exists.class: info

compile: "Compile"
compile.class: step_completed

match: "Same\nshape?"
match.shape: diamond
match.class: info

verified: "Verified"
verified.class: step_completed

local: "Local\ndev?"
local.shape: diamond
local.class: warning

recompile: "Delete old\n& compile new"
recompile.class: step_completed

fail: "Fail\nto start"
fail.class: step_failed

register: "Register\nworker"
register.class: step_started

polling: "Start\npolling"
polling.class: step_completed

start -> exists: "lookup by slug"
exists -> compile: "missing"
exists -> match: "found"
match -> verified: "same shape"
match -> local: "different"
local -> recompile: "local"
local -> fail: "production"
compile -> register
verified -> register
recompile -> register
register -> polling
```

When a worker starts, it:

1. **Extracts the complete flow shape** from the imported TypeScript definition
2. **Compiles the flow if missing** - PostgreSQL creates the flow, steps, and queue from the shape
3. **Verifies the shape if the flow exists** - PostgreSQL compares the worker's shape with the database definition
4. **Registers the worker** - tracks the edge function, inserts the worker row, then starts polling

## Local Development

In local development, startup compilation enables seamless iteration:

1. **No flow exists** - compiles and creates it
2. **Flow exists with same shape** - uses the existing definition
3. **Flow exists with different shape** - **deletes the existing flow and all its run data**, then compiles the new version

The workflow is fully automatic:

1. You change your flow code
2. Edge runtime detects the change and restarts the function
3. pgflow cron triggers the function via HTTP request
4. Worker starts, detects shape mismatch, deletes old flow, compiles new one

<Aside type="caution">
**Warning:** In local development, recompiling **deletes the existing flow, its runs, task state, and queue** before rebuilding it. This is intentional for fast iteration but means you lose all previous runs.
</Aside>

## Production

In production, shape mismatches leave flow data unchanged and cause the worker to **refuse to start**:

- No flow definition, run, or task data is deleted
- No worker registration is created or changed
- Worker fails with `FlowShapeMismatchError`
- Resolution: create a new concrete flow version (see [Version Flows](/build/version-flows/))

For breaking pgflow upgrades that change the compilation SQL signature, follow the fenced migration procedure in [Update pgflow](/deploy/update-pgflow/#remove-manual-flow-compilation).

## What is "Flow Shape"?

**Shape comparison detects:**
- step slugs and order;
- step types;
- dependencies;
- `if` and `ifNot` patterns;
- `whenUnmet` and `whenExhausted` behavior.

**Shape comparison does not detect:**
- handler implementation changes;
- TypeScript input or output contract changes;
- tunable retry, delay, and timeout options.

## Related

<CardGrid>
  <LinkCard
    title="Worker Lifecycle"
    href="/concepts/worker-lifecycle/"
    description="How workers poll, heartbeat, and restart"
  />
  <LinkCard
    title="Version Flows"
    href="/build/version-flows/"
    description="Create a new flow version when shape or handlers change"
  />
</CardGrid>
