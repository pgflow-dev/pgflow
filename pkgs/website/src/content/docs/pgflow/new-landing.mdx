---
title: pgflow - Supabase Workflow Orchestration
description: Build AI Agent Workflows and Background Jobs on Supabase - All in SQL with Zero Infrastructure
template: splash
hero:
  title: Build AI Agents and Workflows on Supabase
  tagline: Run reliable, parallel workflows on Supabase Background Jobs - zero infrastructure, with database-backed reliability
  image:
    dark: ../../../assets/pgflow-logo-dark.svg
    light: ../../../assets/pgflow-logo-light.svg
  actions:
    - text: Get Started
      link: /pgflow/getting-started/install-pgflow/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/pgflow-dev/pgflow
      icon: github
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Postgres-Native Workflow Engine for Supabase

pgflow supercharges your Supabase project with reliable workflow orchestration:

<CardGrid>
  <Card title="Zero Infrastructure" icon="rocket">
    Run complex workflows directly on Supabase Edge Functions and Background Jobs - no additional servers or message brokers required.
  </Card>
  <Card title="AI Agent Orchestration" icon="star">
    Build multi-step AI agent workflows with parallel execution, dependency management, and reliable state tracking.
  </Card>
  <Card title="Type-Safe Workflows" icon="seti:typescript">
    Define workflows in TypeScript with full type inference between steps, providing compile-time safety and IDE autocompletion.
  </Card>
  <Card title="Postgres-First Design" icon="open-book">
    All workflow state lives in your database with ACID-compliant processing - no external state to manage or sync.
  </Card>
</CardGrid>

## Build AI Agent Workflows in Minutes

Define complex AI workflows with parallel execution and dependencies using a simple, type-safe API:

```typescript
// AI Agent workflow with parallel processing
new Flow<{ userQuery: string }>()
  .step("retrieveContext", async ({ userQuery }) => {
    return await vectorSearch(userQuery);
  })
  .step("generateResponse", ["retrieveContext"], async ({ retrieveContext, run }) => {
    return await callLLM({
      prompt: run.userQuery,
      context: retrieveContext.results
    });
  })
  .step("analyzeSentiment", ["generateResponse"], async ({ generateResponse }) => {
    return await analyzeSentiment(generateResponse.text);
  })
  .step("summarize", ["generateResponse"], async ({ generateResponse }) => {
    return await summarizeWithAI(generateResponse.text);
  })
  .step("saveToDb", ["analyzeSentiment", "summarize", "generateResponse"], async (input) => {
    return await saveResults({
      query: input.run.userQuery,
      response: input.generateResponse.text,
      sentiment: input.analyzeSentiment.score,
      summary: input.summarize
    });
  });
```

## Perfect for Supabase Projects

<CardGrid>
  <Card title="For Supabase Power Users" icon="puzzle">
    Extend your Supabase project with reliable background jobs and workflow orchestration without leaving the Supabase ecosystem.
  </Card>

  <Card title="For AI/LLM Developers" icon="rocket">
    Orchestrate complex AI agent workflows with parallel processing, retries, and state management - all with type safety.
  </Card>

  <Card title="For Solo SaaS Builders" icon="approve-check">
    Zero infrastructure overhead - run production-grade workflows on your existing Supabase project without managing servers.
  </Card>

  <Card title="For Postgres-Native Teams" icon="open-book">
    Built on [PGMQ](https://github.com/tembo-io/pgmq) with all workflow state in your database - monitor and debug with standard SQL queries.
  </Card>
</CardGrid>

## Supercharge Supabase Background Jobs

Create and run workflows with a straightforward process:

1. **Define your workflow** in TypeScript with full type safety and IDE autocompletion
2. **Deploy with one command** - `npx pgflow install` handles migrations and configuration
3. **Run as Supabase Edge Functions** with automatic restarts and zero infrastructure overhead
4. **Start workflows** from your application code, database triggers, or REST APIs

The execution system handles the rest—scheduling steps when dependencies complete, retrying failed tasks, and aggregating results automatically.

## Ready to build AI workflows on Supabase?

Install pgflow in your Supabase project with a single command:

```bash
npx pgflow install
```
