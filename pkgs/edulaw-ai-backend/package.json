{
  "name": "edulaw-ai-backend",
  "scripts": {
    "build": "poetry --ansi install",
    "check": "poetry --ansi check && echo '-> Running pyright' && poetry run pyright",
    "test": "poetry run pytest --color=yes **/*_test.py"
  }
}
