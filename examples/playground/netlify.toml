[build]
  base = "examples/playground"
  command = "cd ../.. && pnpm install --frozen-lockfile && cd examples/playground && pnpm run build"
  publish = ".next"

[build.environment]
  NPM_FLAGS = "--version" # This prevents Netlify from automatically running npm install
  NETLIFY_USE_PNPM = "true"