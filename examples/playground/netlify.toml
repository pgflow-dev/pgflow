[build]
  base = "examples/playground"
  command = "echo 'Build already completed in GitHub Actions'"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--force" # Force npm to install packages even if there are conflicts
  NODE_VERSION = "20.18.1"